"""Phase-10 baseline JSON storage.

Concrete baseline files (e.g. ``v0.1.5.json``) are written by the runner
extension in Plan 10-04 once a real bench run produces canonical numbers.
This package marker only exists so ``importlib.resources.files`` can
locate the directory.
"""
