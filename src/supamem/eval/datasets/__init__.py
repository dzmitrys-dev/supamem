"""Bundled / lazy-fetched dataset loaders for the supamem bench harness.

Subpackage marker required for ``importlib.resources`` lookup of any
shipped fixture files. The actual loaders live alongside per-dataset
metadata modules (e.g. :mod:`longmemeval_meta`, :mod:`longmemeval_loader`).
"""
